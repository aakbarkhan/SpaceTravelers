import React from 'react';

const Rock = (props) => {
  const rocket = props;
  // console.log(rocket);
  return (
    <div>
      <h1>{console.log(rocket[0])}</h1>
    </div>
  );
};
export default Rock;
